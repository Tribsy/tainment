# Phase 1: requirements split into requirements/{base,dev,production}.txt.
# This file stays at the root and re-exports base.txt so deploy.sh, IDEs, and
# tooling that auto-detect "requirements.txt" keep working.
-r requirements/base.txt
