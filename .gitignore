# Environment / secrets
.env
.env.example
CLAUDE.md
.bot.lock
deploy.sh

# Database (contains user data)
*.db
*.sqlite
*.sqlite3

# Logs
*.log
bot.log

# Python cache
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/

# VS Code / Cursor
.vscode/
.cursor/

# MCP
mcp/
mcp-start.log
start-discord-mcp.bat
# scripts/ holds project tooling (hooks, maintenance scripts, deploy helpers).
# Track everything except machine-specific or one-off helpers listed below.
scripts/discord-mcp.ps1
discord-mcp-setup.md

# One-time setup/maintenance scripts (not needed for deployment)
setup_output.txt
casino_setup_output.txt
maintenance_output.txt
CASINO_SETUP.md
devdreams_setup_1.py
frontend-redesign-plan.md


# Build artifacts
target/
# docs/ is project structure (architecture, commands, setup, api). Generated
# Sphinx output, if ever added, goes into docs/_build/ which is excluded below.
docs/_build/

# OS
.DS_Store
Thumbs.db
